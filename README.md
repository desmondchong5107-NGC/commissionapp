# Commission App

A lightweight agent commission calculator for Cash and EPF transactions.

## Files

- `index.html` contains the calculator UI and calculation logic.
- `manifest.webmanifest`, `favicon.svg`, and `service-worker.js` support app-like installation and offline caching.
- `vercel.json` keeps the Vercel deployment simple for this static site.
